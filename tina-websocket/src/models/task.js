// a task from the redis monitoring list
export default class task {
    constructor(name, taskProperties={}) {
       this.name = name //fasttext
       this.taskProperties=taskProperties
       //"training" 
       }
    
    }